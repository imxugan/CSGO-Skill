<!DOCTYPE html>
<html>
    <head>
        <title><%
            if (page.title) {
        %><%= page.title %><%
            } else {
        %>CSGO Skill<%
            }
        %></title>
    </head>
    <body>
